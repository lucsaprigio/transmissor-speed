import styled from 'styled-components';

export const Container = styled.footer`
  width: 100%;

  position: absolute;
  bottom: 0;

  background: var(--blue);

  text {
    font-size: 0.8rem;
    color: var(--shape);
    margin-left: 1rem;
  }
  `;  